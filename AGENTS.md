# Agent Entry Point

The single source of truth for this repository is in `docs/`.

Start here:
- `docs/AGENTS.md` for operating rules and doc navigation
- `docs/architecture.md` for the codebase map
- `docs/where-to-touch.md` for file ownership and "where to edit" guidance
- `docs/decisions.md` for invariants, constraints, and gotchas
- `docs/ui.md` for the Costwise Design System ("Paper and Produce") and UI primitives

Do not duplicate guidance outside `docs/`; update the canonical docs instead.

