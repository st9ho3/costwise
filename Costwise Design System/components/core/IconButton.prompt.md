Square icon-only action — row overflow menus, composer attach, collapse toggles.

```jsx
<IconButton icon={<Icon name="ellipsis" />} label="More" />
<IconButton icon={<Icon name="plus" />} variant="solid" round label="Add ingredient" />
```

`label` is mandatory. Use `active` for toggled sidebar/segment state.
