The list row for ingredients, dishes, invoices and suppliers.

```jsx
<DataRow thumb={<Icon name="carrot"/>} title="Carrots, loose" subtitle="Local market · 12 Aug" amount="€1.10" amountNote="per kg" />
<DataRow card title="Metro invoice" subtitle="4 items flagged" end={<Badge tone="watch">Check</Badge>} onClick={()=>{}} />
```

Divider list by default (rows inside one Card); `card` for a feed of separate rows.
