Switches between views of the same data.

```jsx
<Tabs value={tab} onChange={setTab} items={[{value:'week',label:'This week'},{value:'month',label:'Month',count:4}]} />
<Tabs variant="underline" value={t} onChange={setT} items={['Dishes','Ingredients','Suppliers']} />
```
